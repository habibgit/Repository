machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t ahmadhamadeh/adriano .

deployment:
  hub:
    branch: master
    commands:
      - docker login -e $habib.ali@accenture.com -u habibgit -p $DOCKER_PASS
      - docker push habibgit